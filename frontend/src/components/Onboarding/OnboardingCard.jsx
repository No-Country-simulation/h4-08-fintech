/* eslint-disable no-unused-vars */
/* eslint-disable react/prop-types */

const OnboardingCard = ({onNext}) => {
    return (
        <div>
            NEXT 2
        </div>
    );
};

export default OnboardingCard;